# ==================================================================
# Hitster Local Development Environment
# ==================================================================

local_resource(
    "dev",
    serve_cmd = ["bun", "dev"],
    serve_dir = "../../src/nuxt",
    labels = ["Frontend"],
    auto_init = True,
)
